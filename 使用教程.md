# Controller SDK 二进制包

本包提供头文件、`libcontroller_sdk.so` 和 CMake 配置，用于在 Linux 上链接本 SDK。不包含源码。

| 包名 | 适用机器 |
|------|----------|
| `controller-sdk-binary_<ver>_x86_64.tar.gz` | x86_64 |
| `controller-sdk-binary_<ver>_aarch64.tar.gz` | aarch64（ARM 控机） |

架构必须与包名一致。在目标机上从源码执行 `./scripts/package_binary_sdk.sh` 生成对应包。ARM 板步骤见同目录 `ARM编译打包.md`。

依赖：`build-essential`、`cmake`（≥ 3.14）、`libeigen3-dev`。建议 Ubuntu 20.04 / 22.04 / 24.04，glibc 尽量接近打包机。

```bash
sudo apt update
sudo apt install -y build-essential cmake libeigen3-dev
```

---

## 1. 解压

```bash
tar xzf controller-sdk-binary_1.3.1_x86_64.tar.gz   # 或 *_aarch64.tar.gz
cd controller-sdk-binary_1.3.1_x86_64
export SDK_ROOT=$PWD
```

```
controller-sdk-binary_<ver>_<arch>/
├── README.md
├── 使用教程.md              # 本文件
├── ARM编译打包.md
├── 实时性测试结果.txt
├── include/controller_sdk/
├── bin/
├── lib/
│   ├── libcontroller_sdk.so*
│   ├── libethercat.so*      # 真机控制需要
│   └── cmake/controller_sdk/
└── share/controller-sdk/
    ├── scripts/install_igh.sh
    └── udev/
```

---

## 2. 使用方式

**不解压到系统（试用）：**

```bash
export SDK_ROOT=$PWD
export LD_LIBRARY_PATH=$SDK_ROOT/lib:$LD_LIBRARY_PATH
```

新开终端需重新 export，或写入 `~/.bashrc`。

**装到 `/usr/local`：**

```bash
sudo cp -a lib/libcontroller_sdk.so* lib/libethercat.so* /usr/local/lib/
sudo cp -a include/controller_sdk /usr/local/include/
sudo mkdir -p /usr/local/lib/cmake
sudo cp -a lib/cmake/controller_sdk /usr/local/lib/cmake/
sudo ldconfig
```

装好后一般不用再设 `LD_LIBRARY_PATH`。

---

## 3. 最小工程

```bash
mkdir -p ~/hello_sdk && cd ~/hello_sdk
```

`main.cpp`：

```cpp
#include <controller_sdk/scurve_planner.h>
#include <controller_sdk/mock_servo.h>
#include <iostream>

int main() {
    SCurveConfig cfg;
    cfg.max_velocity = 1.0;
    cfg.max_acceleration = 5.0;
    cfg.max_jerk = 50.0;

    SCurvePlanner planner;
    if (!planner.plan(0.0, 1.0, cfg)) {
        std::cerr << "规划失败\n";
        return 1;
    }
    std::cout << "S曲线时长: " << planner.duration() << " s\n";

    MockServoDrive servo;
    servo.enable();
    servo.setTargetPositionRT(1000);
    servo.realtimeCycle();
    std::cout << "Mock 位置: " << servo.getActualPosition() << "\n";
    return 0;
}
```

`CMakeLists.txt`：

```cmake
cmake_minimum_required(VERSION 3.14)
project(hello_sdk LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)

find_package(controller_sdk REQUIRED)

add_executable(hello_sdk main.cpp)
target_link_libraries(hello_sdk PRIVATE controller_sdk::controller_sdk)
```

编译：

```bash
cmake -S . -B build \
  -Dcontroller_sdk_DIR=$SDK_ROOT/lib/cmake/controller_sdk
cmake --build build -j
./build/hello_sdk
```

已安装到 `/usr/local` 时可省略 `-Dcontroller_sdk_DIR=...`。

---

## 4. 真机 EtherCAT

开发机只跑规划 / Mock 可跳过。控机需要 IgH 内核模块（与本机 `uname -r` 绑定）：

```bash
cd $SDK_ROOT
sudo ./share/controller-sdk/scripts/install_igh.sh --with-modules
# 已有用户态、只换了内核:
# sudo ./share/controller-sdk/scripts/install_igh.sh --modules-only
```

网卡 MAC（文件以本机为准）：

```bash
ip a
sudo nano /etc/sysconfig/ethercat    # 或 /etc/ethercat.conf
# MASTER0_DEVICE="xx:xx:xx:xx:xx:xx"
# DEVICE_MODULES="generic"
```

ARM 板：EtherCAT 用 eth0，SSH 用 wlan0。

```bash
sudo /etc/init.d/ethercat start
ethercat master
ethercat slaves
```

业务代码使用 `EthercatMaster` / `CiA402Motor` 等，链接方式与第 3 节相同。头文件在 `include/controller_sdk/`。

---

## 5. 常见问题

| 现象 | 处理 |
|------|------|
| `cannot open shared object file: libcontroller_sdk.so` | `LD_LIBRARY_PATH=$SDK_ROOT/lib` 或 `sudo ldconfig` |
| `Could not find package controller_sdk` | `-Dcontroller_sdk_DIR=.../lib/cmake/controller_sdk` |
| `ecrt.h` / 链接 `ethercat` 失败 | 确认包内有 `libethercat.so*`，并已设置库路径 |
| 把 x86 包拿到 ARM（或相反） | 换对应架构的包，或在目标机上重新打包 |
| `ethercat slaves` 无设备 | 查 IgH、网卡、线缆；与 SDK `.so` 无关 |
